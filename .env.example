# Purple MCP Server Configuration
# Copy this file to .env and fill in the required values

# Required: Service token for Console and SDL access
# Note: Token must have "Account" or "Site" level permissions (not "Global")
PURPLEMCP_CONSOLE_TOKEN=your_sentinelone_console_token

# Required: Base URL for Scalyr/SentinelOne console, no trailing slash
PURPLEMCP_CONSOLE_BASE_URL=https://your-console.sentinelone.net

# Required for proxy deployments: Bearer token for nginx proxy authentication
# Generate a strong random token with: openssl rand -base64 32
# This token is required when using the nginx reverse proxy (production profile)
# SECURITY: Never use a predictable or default value in production
PURPLEMCP_AUTH_TOKEN=

# Optional: GraphQL API endpoints (defaults shown below)
# PURPLEMCP_CONSOLE_GRAPHQL_ENDPOINT=/web/api/v2.1/graphql
# PURPLEMCP_ALERTS_GRAPHQL_ENDPOINT=/web/api/v2.1/unifiedalerts/graphql

# Environment Type - Controls security behavior (IMPORTANT for production deployments)
# Valid values:
#   - production/prod: TLS certificate verification bypass is FORBIDDEN (enforced)
#   - development/dev/test: TLS bypass allowed with security warnings
#   - staging: TLS bypass allowed with enhanced warnings
# Default: production (if not set) - secure by default
# NOTE: Set to 'development' for local development if needed
PURPLEMCP_ENV=production
